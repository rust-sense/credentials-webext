{
  "name": "@rust-companion/credentials-browser-extension",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel watch src/manifest.json --host localhost --config @parcel/config-webextension",
    "build": "parcel build src/manifest.json --config @parcel/config-webextension",
    "lint": "biome check src",
    "lint:fix": "biome check src --apply"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@parcel/config-webextension": "^2.12.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.4",
    "@types/chrome": "^0.0.266",
    "parcel": "^2.12.0"
  }
}